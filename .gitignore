# Outputs
outputs/logs/
outputs/reports/
outputs/tmp/

# Local caches — derived artifacts, rebuilt on demand.
# (sector_cache.json is tracked because it's small and seeds new machines.)
data/price_cache.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Secrets (if user moves API keys to env)
.env
.env.local
